/**
 * Shared contexts export file
 * Provides easy access to context providers
 * Note: Hooks are now in src/shared/hooks/ following project conventions
 */

export { AuthProvider, AuthContext } from './auth';
