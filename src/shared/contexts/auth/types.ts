/**
 * Authentication types re-export
 * Centralizes type imports for auth context module
 */
export type { User } from '@shared/types/auth'
export type { AuthState } from '@shared/types/auth'
export type { AuthAction } from '@shared/types/auth'
export type { AuthContextValue } from '@shared/types/auth'
